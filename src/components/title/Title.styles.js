import styled from '@emotion/styled';

import { mint } from '../../theme/colors';

export const Logo = styled.div`
    font-family: Arial, Helvetica, sans-serif;
    font-size: .7em;    
    color: ${mint.default};    
`